# My progress

Notes and scratch work as I go through AI Engineering from Scratch.

## 00 — Setup and tooling
- Environment verified with `verify.py --route beginner`: Python 3.12, Git, Node, Rust, Julia, PyTorch (Apple MPS) all pass.
- Practised the git workflow (add/commit/branch/merge) in a separate `git-practice` repo.

## 01 — Linear algebra intuition
- Ran `vectors.py`: dot product, normalization, cosine similarity, rotation, projection, Gram-Schmidt, rank.
- Key idea: a neural network layer is a matrix multiplying an input vector.
